import { useEffect } from "react";
import { navbarItems } from "../utils";
import gsap from "gsap";

const HamburguerMenu = ({ menuOpen }: { menuOpen: boolean }) => {};

export default HamburguerMenu;
